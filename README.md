# grpc_proto_echo_client_server
An echo client-server example using Goolge gRPC and Proto serialisation 

# Build
Make sure gRpc and Proto are installed 
Run make and execute client/server in separate terminals.
